services:
  - type: web
    name: weather-backend
    env: python
    plan: free
    branch: main
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: cd myproject && gunicorn myproject.wsgi --log-file -
